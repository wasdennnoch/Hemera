{
  "name": "hemera",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "rimraf build/* && sass --load-path=\"./node_modules/normalize.css/\" src/index.scss build/index.css"
  },
  "dependencies": {
    "normalize.css": "^8.0.1",
    "rimraf": "^3.0.2",
    "sass": "^1.39.0"
  }
}
